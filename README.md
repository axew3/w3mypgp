# w3mypgp
The plugin uses ML-KEM for secure key exchange and ML-DSA for sender verification.  Your files are encrypted right in your browser using AES-GCM-256. The server only stores ciphered files that nobody can read.  Recipients receive an email link and use their own private keys to verify the sender and decrypt locally..
